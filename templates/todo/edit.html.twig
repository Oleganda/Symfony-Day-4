{% extends 'base.html.twig' %}

{% block title %}Edit Todo{% endblock %}

{% block body %}
    <h1 class="text-center mt-2">Edit Task</h1>
<div class="container mt-2">
    {{ include('todo/_form.html.twig', {'button_label' : 'Update'}) }}
 </div>
    <div class="d-flex justify-content-sm-center"><a class="btn btn-primary ms-5" href="{{ path('app_todo_index') }}">Back to list</a></div>
   
    {{ include('todo/_delete_form.html.twig') }}

{% endblock %}
