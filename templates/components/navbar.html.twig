<nav class="navbar navbar-expand-lg navbar-light bg-light">
           <a class="navbar-brand" href="/"><img src="https://cdn.pixabay.com/photo/2020/02/24/16/12/whiteboard-4876666_1280.jpg" class="rounded-circle ms-3" width="60px"></a>
           <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
               <span class="navbar-toggler-icon"></span>
           </button>
           <div class="collapse navbar-collapse" id="navbarSupportedContent">
               <ul class="navbar-nav mr-auto">
                   {% if is_granted("ROLE_ADMIN") %}
                   <a class="nav-link" href="{{ path('app_todo_index') }}">New Task</a>
                   {% elseif is_granted("ROLE_USER") %}
                   <a class="nav-link" href="{{ path('app_user') }}">All Task</a>
                   {% else %} 
                   <a class="nav-link" href="{{ path('app_register') }}">Regester</a>
                   <a class="nav-link" href="{{ path('app_todo_index') }}">Login</a>
                   {% endif %}
               </ul>
           </div>
       </nav>